---
phase: 01-schema-api-backbone
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "The migration 20260513000000_order_flow.sql is applied to the live Supabase project"
    - "orders table has rejection_reason, status_history, idempotency_key columns in the live DB"
    - "orders_status_check constraint is present and includes 'in_route' and 'rejected'"
    - "push_subscriptions has endpoint column and push_subscriptions_user_endpoint_idx index"
  artifacts:
    - path: "supabase/migrations/20260513000000_order_flow.sql"
      provides: "Already written by plan 01-01; this plan only applies it"
  key_links:
    - from: "supabase/migrations/20260513000000_order_flow.sql"
      to: "Supabase project mdwifcuaekjboukvsnvg"
      via: "mcp__plugin_supabase_supabase__apply_migration"
      pattern: "apply_migration"
---

<objective>
[BLOCKING] Apply the Phase 01 migration to the live Supabase project using the MCP apply_migration tool.

Purpose: The API rewrite (plan 01-04) and service layer (plan 01-05) both reference columns (`rejection_reason`, `status_history`, `idempotency_key`) and constraints that only exist after this migration runs. This plan is a synchronization gate — no subsequent plan should execute until this succeeds.

Output: All schema changes from `20260513000000_order_flow.sql` are live in Supabase project `mdwifcuaekjboukvsnvg`.
</objective>

<execution_context>
@C:\Users\jsuar\OneDrive\Desktop\Nova pasta\verdemar\.planning\phases\01-schema-api-backbone\01-PATTERNS.md
</execution_context>

<context>
@C:\Users\jsuar\OneDrive\Desktop\Nova pasta\verdemar\.planning\phases\01-schema-api-backbone\01-PATTERNS.md
@C:\Users\jsuar\OneDrive\Desktop\Nova pasta\verdemar\.planning\phases\01-schema-api-backbone\01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply migration via Supabase MCP apply_migration</name>
  <files>supabase/migrations/20260513000000_order_flow.sql</files>
  <read_first>
    - supabase/migrations/20260513000000_order_flow.sql (read the full content — pass it verbatim to the MCP tool)
  </read_first>
  <action>
Read the full content of `supabase/migrations/20260513000000_order_flow.sql` then call the Supabase MCP tool:

```
mcp__plugin_supabase_supabase__apply_migration(
  project_id: "mdwifcuaekjboukvsnvg",
  name: "order_flow",
  query: <full SQL content of the migration file>
)
```

Pass the SQL exactly as written in the file. Do NOT modify the SQL. Do NOT strip comments.

If the tool returns an error:
- If the error mentions "column already exists" — the migration is partially applied; re-read the file, remove the specific ADD COLUMN statement for the already-existing column, and re-apply only the remaining statements.
- If the error mentions "constraint already exists" for `orders_status_check` — the DROP CONSTRAINT IF EXISTS should have prevented this; check the actual constraint name in the DB using `list_tables` and use the correct name.
- If any other error — stop and report the exact error message; do not attempt to work around it.
  </action>
  <verify>
    <automated>Use mcp__plugin_supabase_supabase__execute_sql with project_id "mdwifcuaekjboukvsnvg" and query: "SELECT column_name FROM information_schema.columns WHERE table_name = 'orders' AND column_name IN ('rejection_reason','status_history','idempotency_key') ORDER BY column_name;"</automated>
  </verify>
  <acceptance_criteria>
    - MCP apply_migration returns success (no error)
    - `SELECT column_name FROM information_schema.columns WHERE table_name = 'orders' AND column_name IN ('rejection_reason','status_history','idempotency_key')` returns exactly 3 rows: idempotency_key, rejection_reason, status_history
    - `SELECT conname FROM pg_constraint WHERE conname = 'orders_status_check' AND contype = 'c'` returns 1 row
    - `SELECT indexname FROM pg_indexes WHERE tablename = 'orders' AND indexname = 'orders_idempotency_key_idx'` returns 1 row
    - `SELECT column_name FROM information_schema.columns WHERE table_name = 'push_subscriptions' AND column_name = 'endpoint'` returns 1 row
    - `SELECT indexname FROM pg_indexes WHERE tablename = 'push_subscriptions' AND indexname = 'push_subscriptions_user_endpoint_idx'` returns 1 row
  </acceptance_criteria>
  <done>All six verification SQL queries return the expected results. The live database has all Phase 01 schema changes applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP apply_migration → Supabase | DDL executes with superuser privileges; errors are surfaced but partial applies are possible |
| Supabase → production data | The orders table contains live production orders; column additions are non-destructive; the status CHECK replacement with NOT VALID is safe for existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-A-01 | Tampering | orders_status_check DROP + ADD | mitigate | Using `NOT VALID` prevents table lock; `IF EXISTS` on DROP makes it safe if constraint name differs — no data is changed, only the constraint definition |
| T-01-A-02 | Denial of Service | push_subscriptions DROP unique constraint | mitigate | Dropping `push_subscriptions_user_id_key` allows temporarily duplicate user_id rows during the window between DROP and CREATE INDEX; this window is milliseconds in apply_migration — acceptable risk |
| T-01-A-03 | Elevation of Privilege | Unauthorized status manipulation (no API guard yet) | accept temporarily | API guard is added in plan 01-04; the schema constraint (orders_status_check) is a second-layer defense — accepting gap between 01-03 completion and 01-04 deployment because plans 01-04 and 01-05 execute immediately after |
| T-01-A-04 | Duplicate order injection | No idempotency_key constraint yet active | accept temporarily | The partial unique index on idempotency_key is applied in this plan; POST /orders handler updated in 01-04 to use it |
</threat_model>

<verification>
Run all six SQL queries listed in acceptance_criteria via `mcp__plugin_supabase_supabase__execute_sql`. All must return expected row counts. If any fails, the migration was not fully applied — identify the failing statement from the migration file and re-apply.
</verification>

<success_criteria>
All schema changes are confirmed live in the Supabase database. Plans 01-04 and 01-05 can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-api-backbone/01-03-SUMMARY.md`
</output>
